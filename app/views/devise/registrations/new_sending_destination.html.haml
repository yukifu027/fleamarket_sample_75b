= render "/items/confirm-new-login-header"
%body
  .new__main
    .new__main--content
      .new__main--content-center
        %h2.new__single__head.registration 会員情報入力
        = form_with model: @sending_destination, class:"sign-up__form", local: true do |f|
          = render "users/error_messages"
          .form-group
            %div
              %label 送付先氏名(全角)
              %span.form-require 必須
            =f.text_field :destination_first_name, placeholder: "例) 山田",class: 'input-default'
            =f.text_field :destination_family_name, placeholder: "例) 太朗",class: 'input-default'
          .form-group
            %div
              %label 送付先氏名カナ(全角)
              %span.form-require 必須
            =f.text_field :destination_first_name_kana, placeholder: "例) ヤマダ",class: 'input-default'
            =f.text_field :destination_family_name_kana, placeholder: "例) タロウ",class: 'input-default'
          .form-group
            %label  郵便番号(ハイフン無し)
            %span.form-require 必須
            =f.text_field :post_code, placeholder: "例) 1234567",class: 'input-default'
          .form-group
            %label 都道府県
            %span.form-require 必須
            =f.collection_select :prefecture_code, Prefecture.all, :id, :name, prompt: "--", class: 'input-default'
          .form-group
            %label 市区町村
            %span.form-require 必須
            =f.text_field :city, placeholder: "例) 横浜市青区",class: 'input-default'
          .form-group
            %label 番地
            %span.form-require 必須
            =f.text_field :house_number, placeholder: "例) 青山1-1-1",class: 'input-default'
          .form-group
            %label 建物名
            %span.form-require-n 任意
            =f.text_field :building_name, placeholder: "例) 柳ビル103",class: 'input-default'
          .form-group
            %label  電話
            %span.form-require-n 任意
            =f.number_field :phone_number, placeholder: "例) 09012345678",class: 'input-default'
          .l-single-content
            .form-group
              %p.l-single-text.text-center
              「登録する」のボタンを押すことにより、
              = link_to '#', class: "new_account" do
                利用規約
              に同意したものとみなします
            = f.submit "登録する", class: 'btn-blue'
            .form-group.text-right
              %p
                = link_to "#", class: "new_account" do
                  本人情報の登録について
                  %i.icon-arrow-right
            .form-group.recaptcha_term
              %p
                This site is protected by reCAPTCHA and the Google
                Privacy Policy
                and
                = link_to "#", class: "new_account" do
                  Terms of Service
                apply.
= render "/items/confirm-new-login-footer"